import { ref } from 'vue';

export const userInfo = ref({} as any);

export const userMenus = ref({} as any);

export const userProjectList = ref([] as any);

export const userCurrentProject = ref({} as any);
