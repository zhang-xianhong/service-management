{
  "name": "service-architecture-management",
  "private": true,
  "version": "0.0.2",
  "scripts": {
    "release": "standard-version"
  },
  "devDependencies": {
    "@commitlint/cli": "^12.0.0",
    "@commitlint/config-conventional": "^12.0.1",
    "husky": "^4.3.8",
    "lerna": "^3.22.1",
    "standard-version": "^9.1.1"
  },
  "husky": {
    "hooks": {
      "commit-msg": "node ./commit-msg $HUSKY_GIT_PARAMS && commitlint -E HUSKY_GIT_PARAMS"
    }
  }
}
